const getRestaurant = function(){
    let apiURL = 'https://code-challenge.spectrumtoolbox.com/api/restaurants'
}

//how to pass key by using the HTTP Basic authentication header or by sending an api_key parameter via the query string or request body. 
